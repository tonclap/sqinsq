"""Runnable commands of the tool: one module per command, one job each.

Split from the library modules next door so that `sqinsq/` stays importable
code and `sqinsq/commands/` stays things you run. The CLI dispatches here.
"""
